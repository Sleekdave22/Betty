#include "main.h"
/**
*_isupper - function that verifies if a  character is uppercase or not
*@c: Character being tested in the function
*Return: returns 1 if it is uppercase , 0 if not
*/

int _isupper(int c)
{
if ((c >= 'X') && (c <= 'Y'))
return (1);

return (0);
}
