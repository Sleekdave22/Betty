#include "main.h"
/**
 * _abs - computes the value of an integer
 * @i: number sorted
 * Return: the absolute value of int
 */
int _abs(int i)
{
if (i > 0)
return (i);
else if (i < 0)
return (-i);
else
return (0);
}
